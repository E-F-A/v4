{# app/Resources/views/configipv4/index.html.twig #}
{% extends 'base.html.twig' %}

{% block title %}{% trans %}Welcome to{% endtrans %} eFa -- {{ title|trans }}{% endblock %}
{% block body %}
    <div class="initbody">
        <div class="init">
            <div class="center">
                    <img src="{{ asset('images/logo.png') }}" alt="eFa Logo" />
            </div>
            <h1>{{ title|trans }}</h1>
           <div class="inner-container">
                {{ form_start(form) }}
                <h2>
                    {{ question|trans }}
                </h2>
                <div>
                    {{ form_widget(form.Yes) }}
                    {{ form_widget(form.No) }}
                </div>
                {{ form_widget(form.Back) }} 
            </div>
            {{ form_end(form) }}
            <div class="error-container">
                {{ form_errors(form) }}
            </div>
        </div>
    </div>
{% endblock %}
